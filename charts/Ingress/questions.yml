labels:
  io.cattle.role: cluster # options are cluster/project
categories:
- CI/CD
questions:
- variable: labels
  default: "testapp"
  type: string
  description: "Used for scheduler. Set the key of Ingress Node labels, eg: app:app-1, input app-1"
  label: Set Key of Ingress Node label
  group: "Customized Settings"
- variable: ingress.class
  default: "nginx"
  type: string
  description: "Ingress Class"
  label: Ingress
  group: "Customized Settings"